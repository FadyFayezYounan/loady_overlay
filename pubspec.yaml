name: loady_overlay
description: "A simple and customizable loading overlay package for Flutter applications."
version: 0.0.1
homepage: https://github.com/FadyFayezYounan/loady_overlay.git

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mockito: ^5.4.4

flutter:
